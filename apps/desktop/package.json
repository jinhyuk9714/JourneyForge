{
  "name": "@journeyforge/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "out/main/index.js",
  "scripts": {
    "predev": "pnpm --filter @journeyforge/shared build && pnpm --filter @journeyforge/core build",
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "pretest:e2e": "pnpm --filter @journeyforge/shared build && pnpm --filter @journeyforge/core build && pnpm build && node scripts/ensure-electron.mjs",
    "test:e2e": "playwright test -c playwright.e2e.config.ts",
    "pretest:smoke-real": "pnpm --filter @journeyforge/shared build && pnpm --filter @journeyforge/core build && pnpm build && node scripts/ensure-electron.mjs && pnpm --filter @journeyforge/core exec playwright install chromium",
    "test:smoke-real": "playwright test -c playwright.real-smoke.config.ts",
    "pretest:smoke-execution-real": "pnpm --filter @journeyforge/shared build && pnpm --filter @journeyforge/core build && pnpm build && node scripts/ensure-electron.mjs && pnpm --filter @journeyforge/core exec playwright install chromium",
    "test:smoke-execution-real": "playwright test -c playwright.real-execution.config.ts",
    "prepackage:mac": "pnpm --filter @journeyforge/shared build && pnpm --filter @journeyforge/core build && pnpm build && node scripts/ensure-electron.mjs && electron-builder install-app-deps",
    "pretest:package-smoke": "pnpm --filter @journeyforge/shared build && pnpm --filter @journeyforge/core build && pnpm build && node scripts/ensure-electron.mjs && electron-builder install-app-deps && pnpm package:mac:dir",
    "test:package-smoke": "playwright test -c playwright.package-smoke.config.ts",
    "package:mac": "electron-builder --config electron-builder.yml --publish never",
    "package:mac:dir": "electron-builder --config electron-builder.yml --dir --publish never",
    "test": "vitest run --passWithNoTests",
    "check-types": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@journeyforge/core": "workspace:*",
    "@journeyforge/shared": "workspace:*",
    "keytar": "^7.9.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@types/node": "^24.0.15",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.5.2",
    "autoprefixer": "^10.4.21",
    "electron": "^36.3.1",
    "electron-builder": "^26.0.12",
    "electron-vite": "^3.1.0",
    "jsdom": "^26.1.0",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17",
    "vite": "^6.3.5",
    "vite-tsconfig-paths": "^5.1.4"
  }
}
